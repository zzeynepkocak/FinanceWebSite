/**
 * Keycloak JS adapter artık kullanılmıyor.
 * Kimlik doğrulama KeycloakProvider içindeki Direct Access Grant akışı
 * ile kendi login formumuz üzerinden yapılıyor (localhost:3000).
 *
 * Bu dosya geriye dönük import uyumluluğu için tutulmaktadır.
 */
export {}
